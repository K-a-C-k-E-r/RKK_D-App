"use client";
import React, { FC } from "react"
import { DiscoverPage } from "@/modules/discover";


interface Props {
}

const Page: FC<Props> = (props) => {
    const { } = props;
    return (
        <DiscoverPage />
    )
}

export default Page